{
  "name": "api",
  "version": "1.0.0",
  "description": "Personnal REST API",
  "main": "build/index.js",
  "scripts": {
    "update": "git fetch origin main && git pull origin main && rm -rf build/ && yarn && tsc"
  },
  "repository": "https://github.com/iwa/api.git",
  "author": "iwa",
  "dependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.1",
    "@types/shelljs": "^0.8.8",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-bearer-token": "^2.4.0",
    "shelljs": "^0.8.4"
  }
}
